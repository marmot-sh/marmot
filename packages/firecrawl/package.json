{
  "name": "@marmot-sh/firecrawl",
  "version": "0.9.0",
  "description": "@marmot-sh adapter for Firecrawl — Web scraping, crawling, search, and structured extraction.",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:all": "vitest run --passWithNoTests",
    "clean": "rm -rf dist",
    "lint": "eslint src",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "check": "pnpm lint && pnpm check-types && echo \"✅ All checks passed\" || { echo \"❌ Checks failed\"; exit 1; }"
  },
  "dependencies": {
    "@marmot-sh/core": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@marmot-sh/tsconfig": "workspace:*",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
